# -- general
setw -g xterm-keys on
set -s escape-time 0
set -sg repeat-time 300
set -s focus-events on
set -g mouse on
set -sg exit-empty on

set -q -g status-utf8 on
setw -q -g utf8 on

set -g visual-activity off
setw -g monitor-activity off
setw -g monitor-bell off

set -g history-limit 100000

set-option -g base-index 1
set-option -g renumber-windows on

# Fetch the TERM variable and set it in tmux
if-shell 'env | grep -q "^TERM"' "set -g @term \"$(echo $TERM)\""

# ui update
set -g default-terminal "screen-256color"
set-option -ga terminal-overrides ",*256col*:Tc"
set -g status-bg "#e0af68"
set -g status-fg "#1f2335"
set -g window-status-separator ""
set -g status-position top
set -g window-status-style "bg=#e0af68,fg=#565f89"
set -g window-status-format "#[bg=#e0af68,fg=#565f89] #I:#W #[fg=#1f2335]|"
set -g window-status-current-format "#[bg=#e0af68,fg=#1f2335,bold] #I:#W #[fg=#1f2335]|"
set -g window-status-current-style "bg=#e0af68,fg=#1f2335,bold"
set -g window-status-format "#[bg=#e0af68,fg=#565f89] 👿 #I:#W #[fg=#1f2335]|"
set -g window-status-current-format "#[bg=#e0af68,fg=#1f2335,bold] 🦀 #W #[fg=#1f2335]|"
set -g status-right-length 100
set -g status-right "#[bg=#e0af68,fg=#1f2335] %m-%d-%Y %H:%M 🗓️  "
set -g status-right-style "bg=#e0af68,fg=#1f2335"
set -g status-left-length 100
set -g status-left-style "bg=#ff9e64,fg=#1f2335,bold"


# -- prefix
unbind C-b
set -g prefix 'C-Space'

# create session
bind C-c new-session

# window management
bind -n M-o new-window -a -c "#{pane_current_path}"
bind -n M-O break-pane
bind -n M-Q kill-pane

# update "terminal window" title
set-option -g set-titles on

# I rely on zsh to rename window title
setw -g automatic-rename off

# -- windows manage
unbind n
unbind p
unbind 1
unbind 2
unbind 3
unbind 4
unbind 5
unbind 6
unbind 7
unbind 8
unbind 9
unbind 0
bind -r p previous-window
bind -r n next-window

bind 1 select-window -t 1
bind 2 select-window -t 2
bind 3 select-window -t 3
bind 4 select-window -t 4
bind 5 select-window -t 5
bind 6 select-window -t 6
bind 7 select-window -t 7
bind 8 select-window -t 8
bind 9 select-window -t 9
bind 0 select-window -t 10


# -- panes manage
bind k split-window -vb -c "#{pane_current_path}"
bind j split-window -v -c "#{pane_current_path}"
bind h split-window -hb -c "#{pane_current_path}"
bind l split-window -h -c "#{pane_current_path}"

bind -n M-! join-pane -t :1
bind -n M-@ join-pane -t :2
bind -n 'M-#' join-pane -t :3
bind -n 'M-$' join-pane -t :4
bind -n M-% join-pane -t :5
bind -n M-^ join-pane -t :6
bind -n M-& join-pane -t :7
bind -n M-* join-pane -t :8
bind -n M-( join-pane -t :9

bind -r H resize-pane -L 3
bind -r J resize-pane -D 3
bind -r K resize-pane -U 3
bind -r L resize-pane -R 3

# index
unbind \'
# copy mode
unbind [

bind ";" select-pane -L
bind "/" select-pane -D
bind "[" select-pane -U
bind "'" select-pane -R

bind > swap-pane -D
bind < swap-pane -U

# copy mode
bind -n M-v copy-mode
setw -g mode-keys vi


# prevent application like less, vim clear screen after exit
set-window-option -g alternate-screen off

# persistent save
run-shell ~/.tmux/tmux-resurrect/resurrect.tmux
run-shell ~/.tmux/tmux-continuum/continuum.tmux
set -g @continuum-save-interval '360'
